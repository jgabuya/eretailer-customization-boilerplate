// global $
import Layout from '../lib/layout';

class Category extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default Category;