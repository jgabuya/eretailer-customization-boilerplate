// global $
import Layout from '../lib/layout';

class Checkout extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default Checkout;