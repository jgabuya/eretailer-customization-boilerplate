// global $
import Layout from '../lib/layout';

class GuestCheckout extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default GuestCheckout;