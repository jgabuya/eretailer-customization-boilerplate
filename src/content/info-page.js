// global $
import Layout from '../lib/layout';

class InfoPage extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default InfoPage;