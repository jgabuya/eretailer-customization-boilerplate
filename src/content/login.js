// global $
import Layout from '../lib/layout';

class Login extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default Login;