// global $
import Layout from '../lib/layout';

class MyAccount extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default MyAccount;