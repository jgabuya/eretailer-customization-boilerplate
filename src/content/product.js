// global $
import Layout from '../lib/layout';

class Product extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default Product;