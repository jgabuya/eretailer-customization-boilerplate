// global $
import Layout from '../lib/layout';

class Register extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default Register;