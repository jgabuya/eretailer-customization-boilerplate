// global $
import Layout from '../lib/layout';

class SearchResults extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default SearchResults;