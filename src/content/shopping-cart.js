// global $
import Layout from '../lib/layout';

class ShoppingCart extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default ShoppingCart;