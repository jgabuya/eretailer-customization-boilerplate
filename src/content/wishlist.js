// global $
import Layout from '../lib/layout';

class Wishlist extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}

export default Wishlist;