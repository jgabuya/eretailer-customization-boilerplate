// global $
import Layout from '../lib/layout';

export default class FooterBottom extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}