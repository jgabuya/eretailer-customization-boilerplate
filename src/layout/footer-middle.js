// global $
import Layout from '../lib/layout';

export default class FooterMiddle extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}