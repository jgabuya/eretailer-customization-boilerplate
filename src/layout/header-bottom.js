// global $
import Layout from '../lib/layout';

export default class HeaderBottom extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}