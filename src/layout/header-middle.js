// global $
import Layout from '../lib/layout';

export default class HeaderMiddle extends Layout {
    constructor(props) {
        super(props);
    }

    render() {
        super.render();
    }
}